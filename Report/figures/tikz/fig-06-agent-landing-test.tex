\begin{figure}[H]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  line cap=round,
  line join=round,
  panel/.style={draw=gray!32, fill=gray!3, rounded corners=10pt,
    line width=0.55pt, inner sep=11pt},
  block/.style={rounded corners=6pt, align=center, font=\scriptsize\bfseries,
    text width=2.82cm, minimum height=0.90cm, inner xsep=5pt,
    inner ysep=5pt, text=black},
  input/.style={block, draw=accent!75, fill=blue!6},
  context/.style={block, draw=orange!80!black, fill=orange!10},
  candidate/.style={block, draw=purple!65!black, fill=purple!7},
  output/.style={block, draw=green!55!black, fill=green!8},
  guard/.style={block, draw=gray!62, fill=gray!9, text width=12.4cm},
  badge/.style={circle, fill=accent!86, text=white, font=\tiny\bfseries,
    inner sep=1.25pt, minimum size=8.5pt},
  paneltitle/.style={font=\scriptsize\bfseries, text=black!68,
    fill=white, inner sep=2pt},
  arr/.style={-{Latex[length=2.15mm,width=1.65mm]}, line width=0.86pt,
    accent!78, rounded corners=8pt, shorten <=3pt, shorten >=3pt},
  soft/.style={-{Latex[length=2.0mm,width=1.5mm]}, line width=0.74pt,
    gray!62, densely dashed, rounded corners=8pt, shorten <=3pt, shorten >=3pt},
  lab/.style={font=\tiny\bfseries, fill=white, inner sep=1.15pt,
    text=black!72}
]

\node[input] (body) at (0,1.18) {Body-first read\\{\normalfont\scriptsize page sections before chrome}};
\node[context] (rows) at (3.62,1.18) {Schedule context\\{\normalfont\scriptsize match rows and labels}};
\node[candidate] (candidates) at (7.24,1.18) {Watch candidates\\{\normalfont\scriptsize host links, buttons, pages}};
\node[output] (result) at (10.86,1.18) {Handoff or blocker\\{\normalfont\scriptsize hosting path or no-hosting}};
\node[guard] (guard) at (5.43,-0.62) {Pass condition: useful body candidates are
preserved; paginator URLs and header/footer links are not promoted as final evidence.};

\begin{scope}[on background layer]
  \node[panel, fit=(body) (rows) (candidates) (result) (guard)] (landingpanel) {};
\end{scope}

\node[paneltitle, anchor=west] at ([xshift=0.13cm,yshift=-0.13cm]landingpanel.north west)
  {Landing-agent evaluation path};
\foreach \figNode/\num in {body/1,rows/2,candidates/3,result/4} {
  \node[badge, anchor=north west] at ([xshift=0.08cm,yshift=-0.08cm]\figNode.north west) {\num};
}

\draw[arr] (body.east) -- (rows.west);
\draw[arr] (rows.east) -- (candidates.west);
\draw[arr] (candidates.east) -- (result.west);
\draw[soft] (candidates.south) -- node[lab, right=1pt] {review gate} (guard.north);

\end{tikzpicture}}
\caption{Landing-agent test: candidate extraction must preserve body and schedule-row
context before declaring that no hosting path exists.}
\label{fig:eval-agent-landing-test}
\end{figure}
