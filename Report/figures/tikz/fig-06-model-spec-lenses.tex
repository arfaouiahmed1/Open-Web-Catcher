\begin{figure}[!htbp]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  lens/.style={draw=gray!35, fill=white, rounded corners=7pt,
    line width=0.62pt, align=center, font=\scriptsize, text width=2.36cm,
    minimum height=1.25cm, inner sep=5pt},
  blue/.style={lens, draw=accent!70, fill=blue!5},
  evidence/.style={lens, draw=accent!68, fill=blue!5},
  orange/.style={lens, draw=orange!80!black, fill=orange!9},
  cost/.style={lens, draw=accent!55, fill=blue!3},
  arr/.style={-{Latex[length=2.0mm,width=1.45mm]}, gray!58, line width=0.70pt,
    shorten <=3pt, shorten >=3pt},
  note/.style={draw=gray!35, fill=gray!3, rounded corners=5pt,
    line width=0.50pt, font=\scriptsize, text=black!70, align=center,
    text width=10.5cm, inner sep=4.5pt}
]

\node[blue] (context) at (0,0) {\textbf{Context window}\\[-1pt]{\tiny browser state + traces + tools}};
\node[blue] (output) at (2.75,0) {\textbf{Output headroom}\\[-1pt]{\tiny structured evidence without truncation}};
\node[evidence] (cache) at (5.50,0) {\textbf{Cache visibility}\\[-1pt]{\tiny repeated prompts and tool schema reuse}};
\node[orange] (routing) at (8.25,0) {\textbf{Routing identity}\\[-1pt]{\tiny same model/provider across runs}};
\node[cost] (price) at (11.00,0) {\textbf{Price and limits}\\[-1pt]{\tiny batch cost, RPM, TPM, RPD}};

\foreach \a/\b in {context/output,output/cache,cache/routing,routing/price}{
  \draw[arr] (\a.east) -- (\b.west);
}

\node[note] at (5.50,-1.36)
  {The comparison focuses on runtime constraints that affect evidence collection. Exact
  model names are preserved: the captured default is
  \texttt{gemini-3.1-flash-lite}, not a generic ``Gemini 3.1 Flash'' text model.};

\end{tikzpicture}%
}
\caption{Runtime-selection lenses used to compare models for OWC browser-agent evaluation.}\label{fig:model-spec-lenses}
\end{figure}
