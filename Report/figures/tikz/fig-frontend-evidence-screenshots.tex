\owcFixedRotatedFigurePage{frontend-browser-live-view.png}
  {OWC browser live-view evidence screen.}
  {fig:frontend-browser-live-view-screenshot}
  {The browser live view links agent stage progress to rendered page evidence. It shows
  the active stage, captured frame thumbnails, and the page state that produced the
  evidence being reviewed.}

\owcFixedRotatedFigurePage{frontend-email-draft.png}
  {OWC generated takedown-email draft screen.}
  {fig:frontend-email-draft-screenshot}
  {The email draft screen keeps the generated notice reviewable by exposing the provider
  contact, subject, stream URLs, protocol evidence, server labels, hostnames, and
  screenshot links before any human action is taken.}

\owcFixedRotatedFigurePage{frontend-tool-call-json.png}
  {OWC tool-call JSON payload inspection screen.}
  {fig:frontend-tool-call-json-screenshot}
  {The tool-call payload inspector keeps low-level browser observations auditable. The
  operator can inspect the structured result returned by a tool call instead of relying
  only on a final model summary.}
