\begin{figure}[H]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  line cap=round,
  line join=round,
  panel/.style={draw=gray!32, fill=gray!3, rounded corners=10pt,
    line width=0.55pt, inner sep=12pt},
  block/.style={rounded corners=6pt, align=center, font=\scriptsize\bfseries,
    text width=3.1cm, minimum height=0.85cm, inner xsep=5pt,
    inner ysep=4pt, text=black},
  start/.style={block, draw=orange!80!black, fill=orange!9},
  tool/.style={block, draw=purple!65!black, fill=purple!7},
  browser/.style={block, draw=accent!75, fill=blue!6},
  result/.style={block, draw=green!55!black, fill=green!8},
  warn/.style={block, draw=gray!62, fill=gray!9},
  badge/.style={circle, fill=accent!86, text=white, font=\tiny\bfseries,
    inner sep=1.25pt, minimum size=8.5pt},
  paneltitle/.style={font=\scriptsize\bfseries, text=black!68, fill=white,
    inner sep=2pt},
  arr/.style={-{Latex[length=2.15mm,width=1.65mm]}, line width=0.86pt,
    accent!78, rounded corners=8pt, shorten <=3pt, shorten >=3pt},
  soft/.style={-{Latex[length=2.0mm,width=1.5mm]}, line width=0.76pt,
    gray!62, densely dashed, rounded corners=8pt, shorten <=3pt, shorten >=3pt},
  lab/.style={font=\tiny\bfseries, fill=white, inner sep=1.15pt,
    text=black!72}
]

% 1. Initial State
\node[start] (target) at (0,2.90) {Candidate target\\{\normalfont\scriptsize from inspection}};
\node[tool] (choice) at (3.80,2.90) {Choose movement\\{\normalfont\scriptsize link or page action}};

% 2. Diverging Branches
\node[tool] (url) at (8.40,3.90) {URL navigation\\{\normalfont\scriptsize open discovered link}};
\node[tool] (js) at (8.40,1.90) {JavaScript action\\{\normalfont\scriptsize click, scroll, load}};

% 3. Consequent States
\node[browser] (urlstate) at (12.40,3.90) {New document\\{\normalfont\scriptsize wait + settle}};
\node[browser] (jsstate) at (12.40,1.90) {Same page state\\{\normalfont\scriptsize DOM changes}};

% 4. Converging Resolution
\node[result] (inspect) at (16.70,2.90) {Re-inspect\\{\normalfont\scriptsize visible result}};
\node[result] (next) at (20.60,2.90) {Continue route\\{\normalfont\scriptsize or handoff}};

% 5. Bottom Recovery Channel
\node[warn] (recover) at (12.40,-0.20) {Recovery\\{\normalfont\scriptsize popup, focus, back}};


% Background Panels
\begin{scope}[on background layer]
  \node[panel, fit=(target) (choice) (url) (js) (urlstate) (jsstate) (inspect) (next)] (navpanel) {};
  \node[panel, fit=(recover)] (recoverpanel) {};
\end{scope}

% Titles
\node[paneltitle, anchor=west] at ([xshift=0.13cm,yshift=-0.13cm]navpanel.north west) {Navigation action loop};
\node[paneltitle, anchor=west] at ([xshift=0.13cm,yshift=-0.13cm]recoverpanel.north west) {Recovery channel};

% Badges
\node[badge, anchor=north west] at ([xshift=0.08cm,yshift=-0.08cm]target.north west) {1};
\node[badge, anchor=north west] at ([xshift=0.08cm,yshift=-0.08cm]choice.north west) {2};
\node[badge, anchor=north west] at ([xshift=0.08cm,yshift=-0.08cm]url.north west) {3};
\node[badge, anchor=north west] at ([xshift=0.08cm,yshift=-0.08cm]inspect.north west) {4};


% -------------------------------------------------------------
% Pathing Logic 
% -------------------------------------------------------------

% Linear starts
\draw[arr] (target.east) -- (choice.west);

% S-curve diverging branches
\draw[arr] ([yshift=0.18cm]choice.east) to[out=0,in=180] (url.west);
\draw[arr] ([yshift=-0.18cm]choice.east) to[out=0,in=180] (js.west);

% Direct horizontal state transitions
\draw[arr] (url.east) -- (urlstate.west);
\draw[arr] (js.east) -- (jsstate.west);

% S-curve converging branches
\draw[arr] (urlstate.east) to[out=0,in=180] ([yshift=0.18cm]inspect.west);
\draw[arr] (jsstate.east) to[out=0,in=180] ([yshift=-0.18cm]inspect.west);

% Linear continuation
\draw[arr] (inspect.east) -- (next.west);


% -------------------------------------------------------------
% Fall-through Recovery Paths
% -------------------------------------------------------------

% Top document failing downwards
\draw[soft] (urlstate.south) -- (jsstate.north);

% Same page failing into recovery
\draw[soft] (jsstate.south) -- (recover.north);

% Gentle swoop from recovery back into the re-inspection flow
\draw[soft] (recover.east) to[out=0,in=-90]
  node[lab, pos=0.65, right=2pt] {retry} ([xshift=0.25cm]inspect.south);

\end{tikzpicture}
}
\caption{Navigation flow split between direct URL navigation and JavaScript-driven page interaction.}\label{fig:tool-navigation-flow}
\end{figure}