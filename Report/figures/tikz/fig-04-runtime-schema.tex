\begin{figure}[!htbp]
\centering
\resizebox{0.99\textwidth}{!}{%
\begin{tikzpicture}[
  line cap=round,
  line join=round,
  umlbox/.style={
    rectangle split,
    rectangle split parts=2,
    rectangle split draw splits=true,
    rectangle split part fill={blue!6,white},
    draw=accent!76,
    line width=0.55pt,
    rounded corners=2pt,
    inner xsep=5pt,
    inner ysep=4pt,
    text width=2.62cm,
    font=\fontsize{6.1}{7.25}\selectfont,
    rectangle split part align={center,left},
    align=left
  },
  root/.style={umlbox, text width=4.15cm, line width=0.75pt,
    draw=accent!90!black, rectangle split part fill={accent!12,white}},
  routeclass/.style={umlbox, draw=orange!82!black,
    rectangle split part fill={orange!10,white}},
  evidenceclass/.style={umlbox, draw=green!55!black,
    rectangle split part fill={green!8,white}},
  reviewclass/.style={umlbox, draw=purple!65!black,
    rectangle split part fill={purple!7,white}},
  metricclass/.style={umlbox, draw=gray!65,
    rectangle split part fill={gray!10,white}},
  compo/.style={draw=accent!76, line width=0.62pt,
    {Diamond[length=6pt,width=4.6pt,fill=accent!76]}-,
    shorten <=1pt, shorten >=4pt, rounded corners=6pt},
  assoc/.style={draw=accent!66, line width=0.58pt,
    shorten <=2pt, shorten >=2pt, rounded corners=6pt},
  dep/.style={draw=gray!62, line width=0.55pt, dash pattern=on 2.4pt off 1.8pt,
    -{Latex[round,length=1.55mm,width=1.15mm]},
    shorten <=2pt, shorten >=3pt, rounded corners=6pt},
  mult/.style={font=\fontsize{5.4}{6.2}\selectfont\bfseries,
    fill=white, inner sep=1.1pt, text=black!68},
  note/.style={draw=gray!35, fill=gray!3, rounded corners=5pt,
    text width=11.6cm, align=center,
    font=\fontsize{5.8}{6.8}\selectfont, text=gray!72!black,
    inner xsep=5pt, inner ysep=4pt}
]
\newcommand{\attr}[1]{+\,#1\\}

\node[root] (result) at (0,6.05) {%
  {\bfseries PipelineResult}\nodepart{two}%
  \attr{run\_id: str}\attr{url: str}%
  \attr{classification: ClassificationResult[0..1]}%
  \attr{matches: MatchInfo[0..*]}%
  \attr{extraction\_results: ExtractionResult[0..*]}%
  \attr{final\_status: ExtractionStatus}%
  \attr{all\_streams: StreamURL[0..*]}%
  \attr{all\_screenshots: str[0..*]}%
  \attr{provider\_analysis: ProviderInfo[0..*]}%
  \attr{takedown\_emails: TakedownEmail[0..*]}%
  \attr{metrics: RunMetrics[0..1]}%
};

\node[routeclass] (classification) at (-6.25,2.62) {%
  {\bfseries ClassificationResult}\nodepart{two}%
  \attr{url: str}\attr{page\_type: PageType}%
  \attr{confidence: Confidence}\attr{reasoning: str}%
  \attr{agent\_type: AgentType}%
};

\node[evidenceclass] (match) at (-3.12,2.62) {%
  {\bfseries MatchInfo}\nodepart{two}%
  \attr{url: str}\attr{title: str}%
  \attr{channel: str[0..1]}\attr{route: str}%
  \attr{iframes: list[str]}\attr{player\_urls: list[str]}%
  \attr{server\_hints: list[dict]}\attr{metadata: dict[str, Any]}%
};

\node[evidenceclass] (extract) at (0,2.62) {%
  {\bfseries ExtractionResult}\nodepart{two}%
  \attr{url: str}\attr{page\_type: PageType}%
  \attr{status: ExtractionStatus}\attr{servers: list[ServerResult]}%
  \attr{streams: list[StreamURL]}\attr{screenshots: list[str]}%
  \attr{embedded\_urls: list[str]}\attr{agent\_type: AgentType}%
};

\node[reviewclass] (provider) at (3.12,2.62) {%
  {\bfseries ProviderInfo}\nodepart{two}%
  \attr{stream\_url: str}\attr{ip: str}%
  \attr{hostname: str}\attr{org: str}%
  \attr{provider: str}\attr{country: str}%
  \attr{abuse\_email: str}\attr{whois\_raw: str}%
};

\node[metricclass, text width=2.82cm] (metrics) at (6.25,2.62) {%
  {\bfseries RunMetrics}\nodepart{two}%
  \attr{run\_id: str}\attr{url: str}%
  \attr{started\_at: datetime}%
  \attr{finished\_at: datetime[0..1]}\attr{total\_tool\_calls: int}%
  \attr{total\_llm\_calls: int}\attr{total\_tokens\_in/out: int}%
  \attr{estimated\_total\_cost\_usd: float}\attr{model\_usage: ModelUsage[*]}%
  \attr{success: bool}%
};

\node[evidenceclass] (server) at (-1.55,-1.08) {%
  {\bfseries ServerResult}\nodepart{two}%
  \attr{label: str}\attr{source\_url: str[0..1]}%
  \attr{server\_up: bool}\attr{m3u8\_urls: list[str]}%
  \attr{mpd\_urls: list[str]}\attr{mp4\_urls: list[str]}%
  \attr{primary\_stream: str[0..1]}\attr{screenshot\_url: str[0..1]}%
  \attr{embedded\_url: str[0..1]}\attr{status: str}%
};

\node[evidenceclass] (stream) at (1.55,-1.08) {%
  {\bfseries StreamURL}\nodepart{two}%
  \attr{url: str}\attr{protocol: str}%
  \attr{quality: str}\attr{source\_layer: str}%
  \attr{channel\_name: str}\attr{captured\_at: datetime}%
};

\node[reviewclass, text width=3.72cm] (email) at (5.60,-1.08) {%
  {\bfseries TakedownEmail}\nodepart{two}%
  \attr{provider: str}\attr{abuse\_email: str}%
  \attr{channel\_name: str}\attr{subject: str}%
  \attr{body: str}\attr{infringing\_url: str}%
  \attr{stream\_urls: str[0..*]}\attr{screenshot\_urls: str[0..*]}%
};

\node[reviewclass, text width=3.72cm] (streamev) at (5.60,-4.70) {%
  {\bfseries StreamEvidence}\nodepart{two}%
  \attr{stream\_url: str}\attr{protocol: str}%
  \attr{source\_layer: str}\attr{server\_label: str}%
  \attr{channel\_name: str}%
  \attr{screenshot\_urls: str[0..*]}\attr{page\_url: str}%
  \attr{provider\_hostname: str}%
};

\draw[compo, draw=green!55!black] (extract.south) -- ++(0,-0.50)
  -| node[mult, near end, right=1pt] {0..*} (server.north);
\draw[compo, draw=green!55!black] (extract.south) -- ++(0,-0.50)
  -| node[mult, near end, right=1pt] {0..*} (stream.north);

\draw[dep] (provider.south) to[out=-90,in=90]
  node[mult, right=1pt, pos=0.50] {stream\_url} (stream.north);
\draw[dep] (email.north west) to[out=155,in=-72]
  node[mult, above=1pt, pos=0.48] {provider\_info 0..1} (provider.south east);
\draw[compo, draw=purple!65!black] (email.south) --
  node[mult, right=4pt, pos=0.78] {0..*} (streamev.north);

\node[note] at (0,-6.72)
  {\texttt{PipelineResult} lists top-level aggregate fields. Diamonds show nested
  ownership; dashed arrows show references.};

\end{tikzpicture}
}
\caption{Class diagram of the runtime result centered on \texttt{PipelineResult}.}\label{fig:runtime-schema}
\end{figure}
