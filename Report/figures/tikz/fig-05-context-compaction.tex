\begin{figure}[H]
\centering
\resizebox{0.94\textwidth}{!}{%
\begin{tikzpicture}[
  line cap=round,
  line join=round,
  panel/.style={draw=gray!32, fill=gray!3, rounded corners=9pt,
    line width=0.55pt, inner sep=10pt},
  box/.style={rounded corners=6pt, align=center, font=\scriptsize\bfseries,
    text width=3.25cm, minimum height=0.85cm, inner xsep=5pt,
    inner ysep=4pt, text=black},
  before/.style={box, draw=orange!80!black, fill=orange!9},
  keep/.style={box, draw=accent!75, fill=blue!6},
  drop/.style={box, draw=gray!58, fill=gray!9},
  after/.style={box, draw=green!55!black, fill=green!8},
  title/.style={font=\scriptsize\bfseries, text=black!70, fill=white,
    inner sep=2pt},
  arr/.style={-{Latex[length=2.1mm,width=1.55mm]}, line width=0.82pt,
    accent!78, shorten <=3pt, shorten >=3pt},
  soft/.style={-{Latex[length=1.9mm,width=1.35mm]}, line width=0.66pt,
    gray!60, densely dashed, shorten <=3pt, shorten >=3pt},
  lab/.style={font=\tiny\bfseries, fill=white, inner sep=1.1pt,
    text=black!72}
]

\node[before] (before) at (0,1.10) {Before compaction\\{\normalfont\scriptsize example: 8,500 / 10,000 tokens}\\{\normalfont\scriptsize 85\% window use}};
\node[keep] (capsule) at (4.20,1.10) {Continuation capsule\\{\normalfont\scriptsize objective, URL, frontier, evidence, next move}};
\node[after] (after) at (8.40,1.10) {After compaction\\{\normalfont\scriptsize example: 1,400 / 10,000 tokens}\\{\normalfont\scriptsize 14\% window use}};
\node[drop] (drop) at (4.20,-1.05) {Removed from context\\{\normalfont\scriptsize repeated tool chatter, stale broad reads, old screenshots}};

\begin{scope}[on background layer]
  \node[panel, fit=(before) (capsule) (after) (drop)] (mainpanel) {};
\end{scope}
\node[title, anchor=west] at ([xshift=0.13cm,yshift=-0.13cm]mainpanel.north west)
  {Context-window compression at the continuation threshold};

\draw[arr] (before.east) -- node[lab, above=2pt] {threshold reached} (capsule.west);
\draw[arr] (capsule.east) -- node[lab, above=2pt] {new invocation} (after.west);
\draw[soft] (capsule.south) -- node[lab, right=1pt] {summarize and prune} (drop.north);

\end{tikzpicture}
}
\caption{Before-and-after view of OWC context compaction when the model context approaches the configured threshold.}\label{fig:context-compaction}
\end{figure}
