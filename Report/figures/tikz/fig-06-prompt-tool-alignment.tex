\begin{figure}[H]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  line cap=round,
  line join=round,
  stage/.style={draw=gray!35, fill=white, rounded corners=7pt,
    line width=0.60pt, align=center, font=\scriptsize,
    text width=2.55cm, minimum height=1.02cm, inner sep=5pt},
  prompt/.style={stage, draw=accent!72, fill=blue!5},
  context/.style={stage, draw=accent!62, fill=blue!3},
  action/.style={stage, draw=orange!78!black, fill=orange!8},
  evidence/.style={stage, draw=accent!80!black, fill=accent!8},
  note/.style={draw=gray!35, fill=gray!3, rounded corners=6pt,
    line width=0.55pt, align=center, font=\scriptsize, text=black!66,
    text width=10.6cm, inner sep=5pt},
  tag/.style={font=\tiny\bfseries, text=black!70, fill=white,
    rounded corners=3pt, inner xsep=4pt, inner ysep=2pt, draw=gray!30},
  arr/.style={-{Latex[length=2.05mm,width=1.50mm]}, line width=0.80pt,
    accent!75, rounded corners=6pt, shorten <=3pt, shorten >=3pt},
  warn/.style={-{Latex[length=1.90mm,width=1.35mm]}, line width=0.68pt,
    orange!80!black, densely dashed, rounded corners=6pt, shorten <=3pt, shorten >=3pt}
]

\node[prompt] (prompt) at (0,1.00)
  {\textbf{Prompt contract}\\[-1pt]{\tiny ReAct step, JSON fields, stop rule}};
\node[context] (ctx) at (3.15,1.00)
  {\textbf{Context retrieval}\\[-1pt]{\tiny row text, iframes, selectors, network hints}};
\node[action] (act) at (6.30,1.00)
  {\textbf{Interaction}\\[-1pt]{\tiny CSS, XPath, frame path, coordinate fallback}};
\node[evidence] (ev) at (9.45,1.00)
  {\textbf{Evidence check}\\[-1pt]{\tiny screenshot, media trace, provider row}};

\node[tag] (t1) at (0,-0.18) {asks for compact state};
\node[tag] (t2) at (3.15,-0.18) {returns actionable targets};
\node[tag] (t3) at (6.30,-0.18) {uses the same targets};
\node[tag] (t4) at (9.45,-0.18) {verifies visible change};

\node[note] (bad) at (4.73,-1.44)
  {A failure can come from a contract mismatch: the model chooses a sensible action, but
  the selector, XPath, frame path, or coordinate no longer matches the inspected browser state.};

\draw[arr] (prompt.east) -- (ctx.west);
\draw[arr] (ctx.east) -- (act.west);
\draw[arr] (act.east) -- (ev.west);
\draw[arr, gray!60] (t1.east) -- (t2.west);
\draw[arr, gray!60] (t2.east) -- (t3.west);
\draw[arr, gray!60] (t3.east) -- (t4.west);
\draw[warn] (bad.north west) -- ++(0.55,0.42) -| (ctx.south);
\draw[warn] (bad.north) -- ++(0,0.42) -| (act.south);
\draw[warn] (bad.north east) -- ++(-0.55,0.42) -| (ev.south);

\end{tikzpicture}%.
}
\caption{Prompt and tool-family alignment contract used during browser-agent evaluation.}\label{fig:prompt-tool-alignment}
\end{figure}
