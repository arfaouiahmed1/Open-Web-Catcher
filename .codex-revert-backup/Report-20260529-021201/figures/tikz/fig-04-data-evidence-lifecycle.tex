\begin{figure}[!htbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=1.25cm,
  stage/.style={draw=accent!80, fill=blue!5, rounded corners=4pt,
    minimum width=3.2cm, minimum height=1.05cm, align=center, font=\small\bfseries},
  record/.style={draw=gray!60, fill=gray!8, rounded corners=3pt,
    minimum width=3.25cm, minimum height=0.85cm, align=center, font=\footnotesize},
  output/.style={draw=green!45!black, fill=green!6, rounded corners=4pt,
    minimum width=3.25cm, minimum height=1.0cm, align=center, font=\small\bfseries},
  arrow/.style={->, >=Stealth, thick, accent}
]

\node[stage] (seed) {Seed URL\\and run record};
\node[stage, right=of seed] (route) {Classification\\and route};
\node[stage, right=of route] (agent) {Specialist\\agent work};
\node[stage, right=of agent] (tools) {Browser tools\\and provider tools};

\node[record, below=1.2cm of agent, xshift=-2.25cm] (events) {Runtime events\\stage timeline};
\node[record, right=0.55cm of events] (toolcalls) {Tool calls\\inputs and outputs};
\node[record, right=0.55cm of toolcalls] (modelcalls) {Model calls\\tokens and cost};

\node[record, below=1.05cm of events] (screens) {Screenshots\\visible state};
\node[record, right=0.55cm of screens] (streams) {Streams and embeds\\media evidence};
\node[record, right=0.55cm of streams] (provider) {Provider and channel\\review context};

\node[output, below=1.3cm of toolcalls] (bundle) {Evidence bundle\\reviewable result};
\node[output, right=1.25cm of bundle] (decision) {Operator decision\\accept, rerun, partial, reject};

\draw[arrow] (seed) -- (route);
\draw[arrow] (route) -- (agent);
\draw[arrow] (agent) -- (tools);
\draw[arrow] (route) |- (events);
\draw[arrow] (agent) -- (events);
\draw[arrow] (tools) -- (toolcalls);
\draw[arrow] (agent) -- (modelcalls);
\draw[arrow] (tools) |- (screens);
\draw[arrow] (tools) |- (streams);
\draw[arrow] (streams) -- (provider);
\draw[arrow] (events) -- (bundle);
\draw[arrow] (toolcalls) -- (bundle);
\draw[arrow] (modelcalls) -- (bundle);
\draw[arrow] (screens) -- (bundle);
\draw[arrow] (streams) -- (bundle);
\draw[arrow] (provider) -- (bundle);
\draw[arrow] (bundle) -- (decision);

\end{tikzpicture}%
}
\caption{Data and evidence lifecycle from seed URL to reviewable operator decision.}
\label{fig:data-evidence-lifecycle}
\end{figure}
