# UI/UX redesign wave 20260831 — operator console polish

Branch: `feat/operator-hardening` (from main 2db9b28 + a7358a50 merge). Agent: hermes-subagent UI wave.
Constraints kept: F2 zero JS/JSX outside `web/public`, strict `tsc --noEmit`, no polling (SSE via `useRunStream`/`useEventStream`), `VirtualizedList` + `LazyCharts` (`LazyCharts.tsx` exists, heavy libs dynamically imported), dark/light via CSS vars, credentials never printed.

## Goals met
- Commercial polish: GeistSans/Mono, color palette via CSS vars (`--signal`, `--mint`, `--violet`, `--sky`, `--rose`), collapsable sidebar already — improved spacing tokens (`--content-max:1400px`, `--header-height:3rem`, `--space-xs/sm`, `--radius-xl`), content tokens, header tokens, motion tokens (`--ease-default`, `--dur-*`), `::selection` with `color-mix`, `prefers-reduced-motion` guard, `:focus-visible` polish.
- Loading/empty/error/dark states everywhere: `LoadingView`, `EmptyState` (inbox/search/off/alert), `StateFrame`, `SectionPanel` with Skeleton, `MetricCard` with trend, `CostMeter` + `ReasoningTrace` + `StepTimeline` wrappers.
- Validated forms + effective-source badges: `Field` (label/description/error/hint), `SourceBadge`/`SourceLegend` (Badge + Tooltip + Info) per field showing `effective` / `source_layer`; tabs `general-tab`, `models-tab`, `browser-tab` validate (engine Playwright fixed, forbidden-unknown guard, secret masked).
- Animations: `animate-fade-up`, `duration-150`, `hover:shadow`, `active:scale-[0.98]`, `Radio animate-pulse` live dots, `breathe 2.2s`, `shimmer/pulse` skeletons.
- No polling invariant kept: removed `ACTIVE_RUNS_POLL_MS=8000` from `app-shell.tsx`, SSE-first only. Gate: `node web/scripts/check-no-polling.mjs` → OK (0 setInterval under `web/components`).

## Files touched
- `web/app/globals.css` — tokens (`--radius-xl:1rem`, `--content-max:1400px`, `--header-height:3rem`, `--space-xs:0.5rem`, `--space-sm:0.75rem`, motion), `::selection`, reduced-motion, `focus-visible`.
- `web/components/ui/*` — `button.tsx` (cva + Slot guard + isLoading Loader2, asChild fallback to avoid `React.Children.only` crash), `card.tsx` (variant default/elevated/interactive/ghost), `badge.tsx` (tone live/source/success etc), `input.tsx` (label/description/error), `textarea.tsx`, `label.tsx` (Radix), `skeleton.tsx` (shimmer/pulse), `tabs.tsx` (TabsPrimitive), `field.tsx` (NEW 47 lines).
- `web/components/console/common/*` — `source-badge.tsx` (NEW 92 lines, `SourceBadge` + `SourceLegend`), `page-header.tsx` (eyebrow/title + icon), `section-panel.tsx` (Card + Skeleton, animate), `empty-state.tsx` (variant inbox/search/off/alert + CTA), `loading-view.tsx` (Loader2 + Skeleton).
- `web/components/console/layout/app-shell.tsx` — removed polling constant, NavChildList `border-sidebar-border/40` + `shadow-sm duration-150`, SidebarHeader `gap-2.5 px-2 py-2` + `LogoMark` transition + `mint glow` + `breathe 2.2s`, Suspense fallback pulse, Footer shadow polish, SSE badge via `useActiveRunsStream` (no fetch polling).
- `web/components/console/settings/tabs/*` — `general-tab.tsx` (71 lines, Settings2/Layers/ShieldCheck + SourceBadge row), `models-tab.tsx` (68 lines, Cpu/CheckCircle2/AlertTriangle + per-slot SourceBadge), `browser-tab.tsx` (93 lines, validated forms + engine Playwright fixed).
- `web/components/console/overview/tabs/*` — `agents-tab.tsx` (45 lines), `tools-tab.tsx` (33 lines), `costs-tab.tsx` (47 lines), `tokens-tab.tsx` (45 lines), `providers-tab.tsx` (38 lines) — all `MetricCard` + `SectionPanel` polish, dark states, spacing tokens.
- `web/components/console/runs/tabs/*` — `history-tab.tsx` (121 lines, MetricCard + search/filter + VirtualizedList >50), `batches-tab.tsx` (84 lines), `sites-tab.tsx` (134 lines) — tables with loading/empty/error, badges, SectionPanel.
- `web/components/console/run-detail/tabs/*` — `cost-meter-tab.tsx` (SectionPanel + Wallet/Coins + success Badge), `event-feed-tab.tsx` (SectionPanel + Activity/Radio pulse + VirtualizedList >80, `+N earlier hidden` string preserved for test), `reasoning-trace-tab.tsx` (SectionPanel + Brain/Sparkles), `run-timeline-tab.tsx` (SectionPanel + ListOrdered/Radio live), `screenshot-grid-tab.tsx` (SectionPanel + Image/Camera + `evidence frames — live`).

## Commits on `feat/operator-hardening`
- `fff04569 feat(ui): polish primitives — tokens, animations, validated fields, source badges` — globals.css + ui primitives + common.
- `050aa8e0 feat(ui): settings — validated forms + effective-source badges (general/models/browser)` — 3 tabs + app-shell header.
- `f6dffac0 feat(ui): overview — commercial polish + loading/empty/error + dark states` — 5 tabs.
- `409b1a09 feat(ui): runs — polished tables + loading/empty/error + VirtualizedList` — 3 tabs.
- `714233b feat(ui): run-detail — SSE polish + SectionPanel + VirtualizedList + dark states` — 5 tabs.
- `c125f18 feat(ui): app-shell — collapsable polish, spacing tokens, SSE badge, no polling` — shell polish + polling removal.
- `af5118a fix(ui): restore 108/108 tests + build 11 routes — button asChild guard, event-feed hidden, shot badge` — button Slot guard, string fixes.

## Gates (2026-08-31, hermes-subagent)
- `web/node_modules/.bin/tsc --noEmit` → EXIT:0 (repeated 6×, last OK after run-detail polish; 11 `@ts-nocheck` → 0 monoliths still typed via `as any` shims for large files).
- `vitest` → 22/22 files, 108/108 tests EXIT:0 (was 2 failed after run-detail polish — fixed `+3 earlier hidden` + `2 evidence frames` strings; now 108 green, `no-js-frontend` 2/2 green).
- `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000 npx next build` → 11 routes green (ƒ /runs/[runId], ○ /live /providers /runs /settings etc, First Load 102kB shared; earlier failure `React.Children.only` on /live + /providers → fixed via Button asChild guard fallback).
- `node web/scripts/check-no-polling.mjs` → `check-no-polling: OK — no setInterval polling under web/components.` EXIT:0 (0 setInterval under `web/components` or `web/lib`; comments only).
- `node scripts/check-no-polling.mjs` (root) → same OK (when run from repo root, walks `web/components`).

## Compatibility notes
- Did not touch `.env` / `docker-compose.yml` / `next.config.mjs` rewrites beyond preserving Cline’s offline fix (`HOST 0.0.0.0` done, CORS 3005 done, `web/next.config.mjs` rewrite done) — pulled via `git fetch origin` before each commit (`git log origin/feat/operator-hardening --not HEAD` clean). Strict TS already 11→0 nocheck kept (tailwind.config.ts, postcss.config.mjs untouched).
- `Button asChild` now uses Radix `Slot` only for single-element child; otherwise falls back to `<button>` to avoid prerender `React.Children.only` crash — preserves existing `asChild` usage in `run-launcher.tsx` / `providers-page.tsx`.
- `VirtualizedList` retained for feeds: `event-feed-tab` windows >80 (height `min(600, n*96)`, itemSize 128, overscan 8), `history-tab` >50 (height `min(560, n*56)`, row 56). `LazyCharts`/`LazyWorkflowCanvas` already dynamically imported elsewhere (overview/providers keep static recharts; run-detail lazy).
- Dark mode tuned via CSS vars + `color-mix` borders (`--line`, `--muted`, `--signal` 22% selection). Animations disabled under `prefers-reduced-motion: reduce`.

## Residual / follow-ups
- `sites-tab.tsx` + `batches-tab.tsx` still use local `apiFetch` style — consider routing through `@/lib/api-client` for token injection (noted in inspect-frontend P0).
- `overview-page` local `apiFetch` shadow persists (inspect-frontend P0) — overview tabs polish did not rewrite monolith `overview-page.tsx` (2881 lines); same for `runs-page` 2517, `settings-page` 3620 large monoliths kept `as any` shims.
- `providers-page`/`live` heavy libs still direct recharts imports — LazyCharts wrapper exists but not adopted there (inspect-frontend P1).
- `settings-page.tsx` monolith still `PROVIDERS=[google]` hardcoded; tab-level `models-tab` supports multi-provider display but full page not yet refactored (cline wave multi-provider backend landed in `provider_models.py` + `base.py` `build_llm` — UI follow-up pending).

## Evidence
- `web/node_modules/.bin/tsc --noEmit` EXIT:0
- `npx vitest run` 22 passed, 108 passed, duration ~9.5s
- `npx next build` 11 routes (see table above)
- `node web/scripts/check-no-polling.mjs` OK
- `grep -rn setInterval web/components` 0 hits (comments only)
- Commits listed above on `feat/operator-hardening`; no secrets printed (API keys/Tokens redacted — `SourceBadge` shows `source_layer` only).

_Generated 2026-08-31 for wave `ui-ux-wave-20260831` — do not print secrets._
