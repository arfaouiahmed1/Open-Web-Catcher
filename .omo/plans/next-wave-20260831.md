# Next wave — 20260831 inspection-driven plan (enriched)

Branch: `feat/operator-hardening` (from `main` a7358a50 / 2db9b286). Swarm: 5 inspectors `deleg_8367e671` + 3 UI/UX `deleg_08c410d0` + Cline `1788136445017_5muq3/1788138704884` (`z-ai/glm-5.3-flash`). This is the single source of truth — enriched after 5 `inspect-*.md` landed (25+8+8+10+0 findings) + live gate rerun `inspection-20260831.md` (27 findings: 6 P0/7 P1/8 P2/6 P3). Previous skeleton `6d729f13` promoted to full backlog below. Each item cites file:line + repro + fix; verify gates at foot.

## How this was built
- Cline plan `z-ai/glm-5.3-flash` via `cline --json -p` (plan mode, 556 events, 65KB JSON) timed out @180s → fallback manual exhaustive `read_file`/`grep`/`terminal` per inspector.
- Live gates rerun 01:53 UTC+1: `tsc --noEmit` 0, `vitest` 22/108, `pytest -q` 1 failed (test bug, not code), `pytest -m replay -q` 10/10, `check-no-polling` OK, `compose config --quiet` 0, `compose ps` 4 healthy + `owc-web unhealthy×56` (`/api/health` 404 bake), `curl :8000/health` 200, `curl :3005/api/health` 404, `grep -r puppeteer` 0 live, `grep -r site_memory.db` 6 legacy, `docker inspect` 0 secrets, `find web -name "*.js"` 0.
- Reports: `inspect-frontend-20260831.md` (159 lines, 41 rows), `inspect-backend-20260831.md` (37kB, B-1 HIGH), `inspect-docker-20260831.md` (21kB, P0-1 bake), `inspect-tests-20260831.md` (29kB, drift fixed), `inspect-security-docs-20260831.md` (26kB, PASS residuals), `inspection-20260831.md` (cross-checked). Ledger `ledger.jsonl` now 82 lines.

## P0 — must fix before next `aki47:DATE` publish (blocks “online” + cost correctness + Settings)

- [ ] **P0-1 `owc-web` offline — Next.js `/api/health` 404 (standalone bake).** `web/next.config.mjs:1-27` + `Dockerfile.web:27-28` (`NEXT_PUBLIC_API_BASE_URL` at build) + `configs/supervisord.conf`. Repro: `curl http://localhost:3005/api/health` 404, `docker inspect owc-web` health `FailingStreak 56`, `routes-manifest rewrites []` while `next.config` has `rewrites()`, `docker exec owc-web node -e fetch('http://owc:8000/health')` 200 but `fetch('http://127.0.0.1:3001/api/health')` 404 — standalone `server.js` baked `localhost:8000` at build, runtime `http://owc:8000` never re-evaluated. Fix: (a) rebuild `open-web-catcher-web` with `API_BASE_URL=http://owc:8000` available at `next build` (add `ARG API_BASE_URL` + `ENV API_BASE_URL=$API_BASE_URL` before `RUN npm run build` in `Dockerfile.web`), or (b) move healthcheck to `fetch('http://owc:8000/health')`, or (c) add `web/app/api/health/route.ts` that proxies server-side without rewrites. Verify: `OWC_TAG=DATE docker compose build web --no-cache`, `docker compose up -d`, `curl -s http://localhost:3005/api/health` 200, `docker compose ps` `owc-web healthy`. Evidence: commit `fix(docker,web): owc-web /api/health bake — runtime API_BASE_URL`.

- [ ] **P0-2 Chrome `9223` bind `127.0.0.1` → `owc` probe `browser_unhealthy`.** `tools/playwright/mcp-server.js:47 HOST` + `tools/playwright/shared/browser.js` `launchPersistentChrome --remote-debugging-address=127.0.0.1` + `docker-compose.yml:75,122 HOST: 0.0.0.0`. Repro: `curl http://localhost:8000/health` shows `mcp healthy true` but `runtime_preflight launch_ready false blocking_reasons browser_unhealthy http://owc-tools-playwright:9223/json/version Connection refused` — MCP is cross-container via `HOST 0.0.0.0` (fixed) but Chrome itself stays loopback, so `owc` cannot reach `ws://owc-tools-playwright:9223` directly. Fix: either bind Chrome to `0.0.0.0` inside sidecar (`BROWSER_WS_HOST=0.0.0.0` or `--remote-debugging-address=0.0.0.0` when `HOST=0.0.0.0`) or stop probing `BROWSER_WS_ENDPOINT` directly from `owc` and rely on `MCP_SERVER_URL=http://owc-tools-playwright:3001` only (MCP proxies browser). Verify: `docker exec owc curl -s http://owc-tools-playwright:9223/json/version` 200, `curl :8000/health` `launch_ready true` or honest `mcp_only` flag.

- [ ] **P0-3 Settings `PROVIDERS=[google]` hardcode vs 5-provider backend.** `web/components/console/settings/settings-page.tsx:52 PROVIDERS`, `providersToLoad`, `loadProviderCatalog`, `src/utils/provider_models.py PROVIDER_METADATA` (now 5). Repro: Settings → Models tab shows only Gemini while `GET /ui/provider/models?provider=openai` would return rows. Fix: derive `PROVIDERS` from `GET /ui/provider/models` catalog or import `PROVIDER_METADATA` mapping; render per-agent `AGENT_SLOTS` dropdowns with `ProviderSelect` + free-form `ModelInput` for litellm. Verify: `tsc --noEmit` 0, `vitest` 22/108, `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000 npm run build` 11 routes.

- [ ] **P0-4 `overview-page` local `apiFetch` shadows token-aware client.** `web/components/console/overview/overview-page.tsx:69` `async function apiFetch(path:any)` does `fetch(apiUrl(path))` without `Authorization` → authed deploy 401 → overview blank (repro: set `OWC_TOKEN` in localStorage, reload `/`, network tab shows no `Authorization`). Fix: delete local (lines 69-85), `import { apiFetch } from "@/lib/api-client"` (injects `Authorization: Bearer …` + `cache:no-store` + `signal`). Same pass add `AbortController`.

- [ ] **P0-5 `app-shell` offline dot — raw `fetch` no token, no abort.** `web/components/console/layout/app-shell.tsx:38,60` `fetch(apiUrl("/ui/overview"))` on every `owc:run-state-changed` + `visibilitychange` — fails behind auth, stale `activeRuns` badge + false offline dot (repro: private mode login, badge 0). Fix: `apiFetch("/ui/overview")` + `AbortController` + debounce 200ms; delete dead `ACTIVE_RUNS_POLL_MS=8000`.

- [ ] **P0-6 `history-tab` VirtualizedList dead threshold + `run_agent_loop` provider hardcode (co-joined).** (a) `web/components/console/runs/tabs/history-tab.tsx:92-111` `rows.slice(0, pageSize)` where `pageSize=25` → `items.length` never >50 → virtualization never triggers (repro: `total=200` profiler shows no `react-window`). Fix: pass unsliced `rows` to `VirtualizedList height=320 itemSize=48` + `itemKey`. (b) `src/agents/base.py:652-682 run_agent_loop` hardcodes `provider="google_genai"` while `build_llm` correctly routes per-agent → wrong pricing/context/caching for non-Google (repro: `pytest -q` shows `build_llm` openai path green but `run_agent_loop` pricing still Gemini). Fix: `selection = resolve_agent_model_selection(settings, agent_id); effective_provider = selection.provider or settings.llm_provider; provider = {"google":"google_genai","openai":"openai","anthropic":"anthropic","openrouter":"openrouter","nvidia":"nvidia_nim"}[effective_provider]; model_context_window/pricing via effective provider; widen warning allow-list to `SUPPORTED_PROVIDERS`. Verify: `pytest -q` green, `test_agent_model_config_survives_every_supported_provider` rewritten (see P1-1).

## P1 — should fix next wave (user-visible, 7 items)

- [ ] **P1-1 Test bug `test_provider_model_catalog.py:94-104`.** Feeds provider names as agent keys (`{agent: {provider: agent} for agent in SUPPORTED_PROVIDERS}`) where `SUPPORTED_PROVIDERS={google,openai,…}` but agent ids are `classification/landing/hosting/embedded/orchestrator` → `normalize_agent_id("openai")==""` → loop `continue` → always `{'google'}`. Fix: rewrite to `raw={agent: {provider: providers[i%len(providers)]} for i,agent in enumerate(AGENT_MODEL_IDS)}` + assert `<= SUPPORTED_PROVIDERS` + `len==min(len(agents),len(providers))`. Verify `pytest -q` 0 failed.

- [ ] **P1-2 `owc` runs as `user: "0:0"` (root) at runtime.** `docker-compose.yml:155` overrides `Dockerfile:91 USER app (10001)` for Windows NTFS heal — `supervisord` spawns `uvicorn` as root (no `user=app` in `supervisord.conf:12`). Fix: keep `USER app` by default; heal via `gosu app supervisord` after `chown` or split Windows override into `docker-compose.override.yml`. Verify `docker exec owc id` → `app` on Linux, still heals on Windows.

- [ ] **P1-3 Playwright image missing OS upgrade.** `Dockerfile.tools.playwright:36-38` lacks `apt-get upgrade -y` (largest surface: Chrome + noble, 2C/40H residuals) while `Dockerfile:36` + `Dockerfile.web:47 apk upgrade` have it. Fix: add `apt-get upgrade -y && rm -rf /var/lib/apt/lists/*` before `curl/unzip/xvfb` install.

- [ ] **P1-4 Integration tier empty + replay not in CI.** `tests/` 644 tests: 432 unit, 0 integration, 10 replay. `conftest.py` 5→0 masks `pytest -m integration` always green → CI `pytest-integration` vacuous; `replay` not exercised. Fix: tag 5-10 real integration (`test_retention`, `test_site_hints`) with `@pytest.mark.integration` or gate `if: steps.tests_run==0` fail; add `pytest-replay` job `needs: [migration-check] uv run pytest -m replay -q` (slow-allowed).

- [ ] **P1-5 Direct `recharts` imports defeat `LazyCharts` code-split.** `overview-page:522-780`, `providers-page:15-35`, `stream-provider-tab:10-18` import `recharts` directly (180kB parsed + 220kB reactflow) while `LazyCharts.tsx:4-34` wrappers (`LazyAreaTrend`, `LazyWorkflowCanvas`) exist unused. Fix: swap to `LazyAreaTrend`/new `LazyBarTrend` `next/dynamic ssr:false` + skeleton; add CI `next build && du -h .next/static/chunks` gate <350kB.

- [ ] **P1-6 A11y sweep.** Icon-only buttons (`browser-live-view:629,751` refresh/fullscreen/zoom, `runs/tabs` prev/next) lack `aria-label` + `type="button"`; `VirtualizedList` containers lack `role="feed"`/`role="article"` + `aria-live="polite"` for SSE; no `prefers-reduced-motion` guard on `ping 1.6s`. Fix: `jsx-a11y control-hasAssociatedLabel`, add `sr-only` spans, `axe-core` run on `/`, `/runs`, `/runs/[id]`, `/live`, `/settings`.

- [ ] **P1-7 OpenAPI drift needs commit.** `inspect-tests` fixed `openapi.json 174960→177631 (+2671b, 10 fields: background_job_retention_days, observability_enabled, payload_cap_bytes, retention_days_*×5, workflow_max_*×2)` + `web/src/types/api.d.ts 5698 lines` via `python scripts/export_openapi.py && npm run types:gen` — currently unstaged. Fix: `git add openapi.json web/src/types/api.d.ts` + CI will pass `types drift` gate.

## P2 — robustness / polish (8 items)

- [ ] **P2-1 `finished_at IS NULL` orphan pipeline leak.** `src/storage/repositories.py:646-660 cleanup_old_artifacts` filters `finished_at.isnot(None)` → orphaned `pipeline_runs` (crashed before finish) never GC'd. Fix: add `_old_orphan_ids` using `created_at < threshold("runs") AND status IN ("running","queued")`.

- [ ] **P2-2 `memory_db_path` ghost.** `src/utils/config.py:598 memory_db_path="data/site_memory.db"` declared but no live reader (pgvector is live) — `alembic 20260826_0022` only probe. Operator editing it gets silent no-op. Fix: mark `deprecated=True`, stop persisting in `save_yaml`, warn if file exists on startup, schedule unlink.

- [ ] **P2-3 Playwright sidecar NTFS heal fragile.** `scripts/docker/entrypoint.playwright.sh:8-10` only `mkdir -p`, no `if root chown` block (unlike `entrypoint.sh:11-18`). Fix: copy `if [ "$(id -u)"="0" ]` block.

- [ ] **P2-4 Error boundaries + skeletons.** No `ErrorBoundary` around console subtrees — chart throw blanks whole page; 8 `Promise.allSettled` fetches on overview mount no `AbortSignal` per call, no per-card `<Suspense>`. Fix: `components/console/common/error-boundary.tsx` per route + `useSWR`/`AbortSignal` per card + `Skeleton` matrix.

- [ ] **P2-5 Bundle CI hygiene.** `vitest.config.mts:15-20` walks generated `web/src/types/api.d.ts` (17k LOC, 19s transform); no `next-bundle-analyzer`. Fix: `exclude: [...,"src/types/**"]`, add `ANALYZE=true next build` artifact limit.

- [ ] **P2-6 Strict TS debt.** `tsc --noEmit` 0 but `734 any/@ts-expect-error/eslint-disable` across `components/console` + 13 `eslint-disable` headers + 96 `any` in overview. Fix: remove headers, `Record<string,unknown>` + `isRecord` helpers, `tsc --noUncheckedIndexedAccess` on CI.

- [ ] **P2-7 Lifecycle / SSE nits.** `seen_urls: set[str]` unsynchronized if ever off-loop; `wait_until_drained` surfaces only first `worker_errors[0]` (logged siblings, deferred by spec); `max_parallel_hosting_pages` read only at spawn; `use-event-stream` `computeBackoffMs` non-seeded jitter + `seq 0` falsy dropped. Fix: document `seen_urls` loop-thread only, seedable jitter for tests, init `lastSeq=-1`.

- [ ] **P2-8 Docs/CORS/CSP follow-ups.** `.env.example` missing `OWC_WEB_HOST_PORT=3005`, `AUTH_JWT_SECRET`, `MCP_BEARER_TOKEN` (dead gate `mcp-server.js:102-114`); `CLOUDINARY_API_SECRET @dktc…` mis-paste; `starlette 1.0.0→≥1.3.1`, `python-multipart 0.0.24→≥0.31`, `next 15.1→15.5`, `postcss`, `sharp` vulns; `CSP frame-ancestors 'none'` minimal (adequate for API, expand if HTML). Fix per `inspect-security-docs` residuals.

## P3 — deferred / low (6 items, track, don't block)

- [ ] **P3-1 Base digests not pinned** (`FROM python:3.11-slim-bookworm`, `node:20-alpine`, `playwright:v1.60.0-noble` placeholder `@sha256:<TO_FILL>`). Resolve `docker buildx imagetools inspect` → pin.
- [ ] **P3-2 `allowJs:true` + `skipLibCheck:true` masks JS creep.** Set `allowJs:false` (0 `.js` hits) + separate `tsc --skipLibCheck false` gate.
- [ ] **P3-3 Favicon `public/.gitkeep` dead** (assets at `app/favicon.ico`). Remove or add real assets.
- [ ] **P3-4 Retention tick startup-only** (no periodic `run_retention_tick` every 24h). Backlog ADR note or document restart-manual.
- [ ] **P3-5 NVIDIA `/` names bare** (`meta/llama-3.3-70b` → `nvidia_nim/meta/…` prefix). Doc operator should provide fully-qualified or add branch.
- [ ] **P3-6 `MCP_BEARER_TOKEN` / `create_admin.py --password` argv leak / auth timing + bcrypt 72B truncation** — support stdin/env, dummy verify + SHA-256 pre-hash.

## Execution order (swarm assignment, commit per-task)

1. **P0-1 + P0-2 docker/web** → `Dockerfile.web` ARG + Chrome bind + `docker-compose.yml` → commit `fix(docker,web): P0 offline — /api/health bake + Chrome 9223 reachability` — rebuild `open-web-catcher-web` + `owc-tools-playwright` + tag `aki47:DATE`.
2. **P0-6b + P1-1 backend** → `src/agents/base.py:652` + `tests/test_provider_model_catalog.py:94` → `pytest -q` green → commit `fix(agents): P0 run_agent_loop per-provider pricing/context + P1 test bug`.
3. **P0-4 + P0-5 + P0-6a + P0-3 + P1-5 frontend** → `overview-page`, `app-shell`, `history-tab`, `settings-page` + `LazyCharts` → `tsc 0` + `vitest 22/108` + `build 11 routes` + `check-no-polling OK` → commit `fix(web): P0 auth/VirtualizedList/Lazy + P0-3 Settings multi-provider`.
4. **P1-2 + P1-3 infra + P1-7 openapi** → `Dockerfile.tools.playwright` upgrade + `owc` non-root override split + `openapi.json` + `api.d.ts` → `compose config --quiet` 0 → commit.
5. **P1-4 CI + P1-6 a11y** → `ci.yml` integration gate + `pytest-replay` job + `vitest exclude src/types` → `axe-core` sweep → commit.
6. **P2 batch** → orphans/GC, `memory_db_path` deprecate, NTFS heal, error boundaries, bundle CI, strict debt, SSE nits, `.env.example` — each small commit on `feat/operator-hardening`, never `down -v`.
7. **Final re-gate:** `tsc 0` + `vitest 22/108` + `pytest -q` + `pytest -m replay -q 10` + `check-no-polling OK` + `compose config --quiet` + `curl :8000/health 200` + `curl :3005/api/health 200` + `docker compose ps` all healthy → tag `aki47:DATE` + `docker scout` credential-clean.

## Verification ledger
Each P* writes JSONL to `.omo/start-work/ledger.jsonl` + markdown to `.omo/start-work/*.md` + branch commits on `feat/operator-hardening`. No image push until `docker scout` + credential sweep (`grep API_KEY|SECRET|TOKEN` 0 history, `docker inspect` 0 secrets, `.dockerignore` has `.env`) green. Findings sources: `inspect-*.md` 5 reports + `inspection-20260831.md` cross-check + `cline-raw-20260831.jsonl` 556 events.
