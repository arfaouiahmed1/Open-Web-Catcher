# Next wave — 20260831 inspection-driven plan

Branch: `feat/operator-hardening` (from `main` 2db9b28 + a7358a50). Swarm: 8 subagents + Cline `1788136445017_5muq3` (`z-ai/glm-5.3-flash`). This plan is the single source of truth for the next list of changes — P0 offline/CORS, P0 multi-provider, P1 Playwright/docs, P1 console UX, P2 perf/replay. Updated live as `inspect-*.md` reports land.

## How this was built
Live verification 00:34-01:07: `docker compose ps` healthy, `owc→owc-tools-playwright:3001/health 000→200` (HOST 127.0.0.1→0.0.0.0), `http://localhost:3005` vs `UI_CORS_ORIGINS 3000→3005` (`access-control-allow-origin` now echoes 3005), `/api/health 404` until `next.config.mjs` rebuild, `PROVIDER_METADATA` single-entry, `build_llm` Google-gate, `PROVIDERS=[google]`. Cline wave file `.omo/start-work/cline-wave-20260831.md` records the full reproduction.

## P0 — must fix before next deploy (blocks “online” + Settings)
- [ ] **P0-1 Offline/CORS+proxy — verify & close.** Files: `.env` (`OWC_WEB_HOST_PORT=3005`, `UI_CORS_ORIGINS=3005`), `docker-compose.yml` (tools `HOST: 0.0.0.0`, headed `HOST: 0.0.0.0`, `owc-web` healthcheck `node -e fetch(.../api/health)`), `web/next.config.mjs` (`rewrites: /api/health → ${API_BASE_URL||NEXT_PUBLIC_API_BASE_URL}/health`). Verify: `OWC_TAG=20260830 docker compose config --quiet`, `docker compose up -d`, `docker exec owc curl -f http://owc-tools-playwright:3001/health` 200, `curl.exe http://localhost:3005/api/health` 200 (after `docker compose build web` + `up -d web`), `curl -D - http://localhost:8000/ui/overview -H Origin:http://localhost:3005` shows 3005, `docker compose ps` all healthy. Evidence: commit `fix(compose,web): Task 1 offline — CORS 3005 + HOST 0.0.0.0 + /api/health rewrite`.
- [ ] **P0-2 Multi-provider backend — unblock per-agent provider/model.** Files: `src/utils/provider_models.py` (`PROVIDER_METADATA` + `FALLBACK_MODELS` + `_provider_api_key` + `fetch_provider_models` dispatch to `_fetch_openai/_anthropic/_openrouter/_nvidia` — currently google-only, dead fetchers), `src/api/provider_config.py` (`get_ui_provider_models` allow all, `apply_ui_config_update` validate `llm_provider` against allowed set, `api_keys` payload masked for all providers), `src/api/app.py` (`_pricing_sync_provider_ids`/`_provider_api_key_available` + `PUT /ui/providers/models` / `PUT /ui/config` provider validation 3670). Verify: `pytest tests/test_provider_model_catalog.py -k normalize_agent_model_config` + `tests/llm/test_provider.py::test_build_llm_honors_model_override` green, `GET /ui/providers/models?provider=openai` returns live/fallback rows. Commit per-file, update `tests/test_provider_model_catalog.py::test_agent_model_config_normalizes_legacy_non_google_provider` to expect preserved provider.
- [ ] **P0-3 Runtime per-agent LLM routing.** Files: `src/agents/base.py:build_llm` (remove `is_google_genai_model_id` gate for non-google, pick `api_key = _provider_api_key(settings, provider)`, `api_base = openrouter_base_url/nvidia_base_url/llm_base_url`, `provider_prefix` via `_PROVIDER_PREFIXES` (`openai/`, `anthropic/`, `openrouter/`, `nvidia_nim/`), google path keeps `normalize_gemini_model_id` + `resolve_google_model_runtime_profile`). Verify: `build_llm(settings, agent_id="classification")` with `agent_model_config={"classification":{"provider":"openrouter","model":"openai/gpt-4o-mini"}}` uses openrouter key+prefix, `pytest tests/llm/test_provider.py -q`. Commit `feat(llm): per-agent multi-provider routing`.
- [ ] **P0-4 Settings persistence — keys + per-agent config.** Files: `src/utils/config.py` (`Settings.google_api_key/openai_api_key/anthropic_api_key/openrouter_api_key/nvidia_api_key`, `validate_settings_patch`/`persist_settings_patch`/`read_settings_with_sources`), `configs/settings.yaml` (`llm_provider` comment “Supported: google | openai | anthropic | openrouter | nvidia | litellm”), `data/settings.runtime.yaml` (runtime layer). Verify: `PUT /ui/config` with `{"llm_provider":"openrouter","agent_model_config":{"landing":{"provider":"anthropic","model":"claude-sonnet-4"}}}` persists and `GET /ui/config` shows `settings_sources` with `runtime_yaml`. Never log raw keys — masked `***REDACTED` only.

## P1 — user-visible (Settings UX + Playwright + console polish)
- [ ] **P1-1 Settings UI — multi-provider + per-agent slots.** Files: `web/components/console/settings/settings-page.tsx` (`PROVIDERS` currently `[google]` → add openai/anthropic/openrouter/nvidia with `keyEnv` + colors, `providersToLoad = PROVIDERS.map(p=>p.id)`, `loadProviderCatalog` for each, masked `api_keys` display via `payload.api_keys[provider].api_key_set`, per-agent `AGENT_SLOTS` selects with free-form `ModelInput` for litellm fallback). Fix `src/utils/provider_models.py` consumer: `build_model_selection_details` now returns all providers. Verify: `web/node_modules/.bin/tsc --noEmit`, `vitest 22/108`, `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000 npm run build` 11 routes, `node scripts/check-no-polling.mjs`. Commit `feat(web): Settings multi-provider + per-agent provider cards`.
- [ ] **P1-2 Settings UX hardening.** Same page: loading/empty/error/dark, validated forms (`zod` via `validate_settings_patch` 422 surfacing), `effective-source` badges (`settings_sources` → `default|env|base_yaml|runtime_yaml`), save states (dirty tabs via `getDirtyTabs`/`buildServerConfigDraft`). Keep `/* eslint-disable */` headers already on 9 monoliths. Verify same gates.
- [ ] **P1-3 Console providers tab.** Files: `web/components/console/providers/providers-page.tsx`, `web/lib/api-client.ts`. Show live catalog (`/ui/providers/models`), `provider_model_catalog_cache`, `thinking`/`caching` toggles per provider via `normalize_llm_tuning`/`resolve_llm_tuning`.
- [ ] **P1-4 Playwright simplification (you called it “complicated bruh”).** Files: `docker-compose.yml` (already trimmed UBOL duplication — image ENV `OWC_UBOL_*` covers it; document minimal env: `HOST`, `PORT`, `MCP_BROWSER_MODE`, `BROWSER_WS_ENDPOINT`, `OWC_BROWSER_HEADLESS`/`OWC_XVFB_SCREEN` for headed profile), `Dockerfile.tools.playwright` (pinned `mcr.microsoft.com/playwright:v1.60.0-noble`, `PLAYWRIGHT_BROWSERS_PATH`, `shm_size 2g`), `tools/playwright/mcp-server.js` (`HOST` default 127.0.0.1 + comment `[TOOL-C1]`), `configs/supervisord*` (no stray `supervisord.playwright.conf` — only `supervisord.conf` + `tools/playwright/profiles.js`). Docs: `docs/` minimal env table. Verify: `docker compose --profile headed-playwright config --quiet`.
- [ ] **P1-5 Console redesign sweep.** Files: `web/components/console/overview/*`, `runs/*`, `run-detail/*`, `live/*`, `layout/app-shell.tsx` (offline dot now driven by CORS fix), `web/components/ui/*`, `web/lib/*`, `web/app/*`. Keep GeistSans/Mono, CSS vars, `SidebarProvider` collapsable, `MetricCard`/`StateFrame`, `VirtualizedList` + `LazyCharts` dynamic. Gates same as P1-1 plus `docker scout`/`npm audit` already 0 high.

## P2 — completeness & hardening
- [ ] **P2-1 More settings wired & verified.** `retention_days`, `pricing`, `observability`, `browser_mode`, `cost caps` (`workflow_max_cost_usd`, `workflow_max_tokens`), `max_parallel_hosting_pages` — all via `Settings` + `validate_settings_patch` + `ui_config_payload` + Settings tabs. Verify via `pytest -q` full + `pytest -m replay -q`.
- [ ] **P2-2 Replay determinism E2E (T51) + F1–F8 re-gate.** `tests/replay/test_agent_replay.py` 6 + `tests/replay/test_fixture_replay.py` 4, `serve_fixtures.py`, `RunPlan` steps, `alembic upgrade head` idempotent, `pip-audit 45` ADR residual documented.
- [ ] **P2-3 Docs + security.** `docs/adr/*`, `docs/DESIGN.md`, `docs/frontend/README.md`, `.dockerignore` excludes `.env`/`auth.json`, `docker inspect` ENV no secrets, `HOST 127.0.0.1` safe default documented.

## Execution order (swarm assignment)
1. Cline main finishes P0-2/3/4 → commit `feat(settings): multi-provider backend` on `feat/operator-hardening` (already `HOST`/CORS/next.config on that branch).
2. Frontend deep inspectors (deleg_8367e671 task-0..4) finish → enrich `inspect-*.md`, blockers promoted to P0/P1 above.
3. UI/UX + perf workers (deleg_08c410d0) take P1-1..P1-5/P2-1 → commit per-subtask, never `down -v`.
4. Final re-gate: `tsc 0` + `vitest 22/108` + `pytest -q` + `check-no-polling OK` + `compose config --quiet` + `curl :8000/health 200` + `curl :3005/api/health 200`.

## Verification ledger
Each P* writes JSONL to `.omo/start-work/ledger.jsonl` + markdown to `.omo/start-work/inspect-*.md` + branch commits on `feat/operator-hardening`. No image push until `docker scout` + credential sweep green.
